# Optional — enables richer photos, hours, and descriptions via Google Places
# GOOGLE_PLACES_API_KEY=your_key_here

# Data provider: "osm" (default, free) or "google" (requires key above)
PLACES_PROVIDER=osm
